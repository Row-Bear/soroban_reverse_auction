{
  "name": "soroban-reverse-auction",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "clean": "rm -rf .soroban node_modules/.vite",
    "contract_build": "cd ../soroban_auction && soroban contract build",
    "create_deployer": "soroban config identity generate deployer && soroban config identity fund deployer --network testnet",
    "deploy": "soroban contract deploy --network testnet --source deployer --wasm target/wasm32-unknown-unknown/release/soroban_auction.wasm > ../.soroban/auction_id",
    "network": "soroban config network add --global testnet --rpc-url https://soroban-testnet.stellar.org --network-passphrase 'Test SDF Network ; September 2015'",
    "bindings": "cd ../frontend && soroban contract bindings typescript --network testnet --overwrite -contract-id $(cat ../.soroban/auction-id) --output-dir node_modules/soroban_auction_client",
    "postinstall": "npm run network && npm run create_deployer && npm run contract_build && npm run deploy && npm run bindings"
    
  },
  "dependencies": {
    "@stellar/freighter-api": "^1.7.1",
    "astro": "^4.0.7"
  }
}
