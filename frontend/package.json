{
  "name": "soroban-reverse-auction",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "clean": "rm -rf .soroban node_modules/.vite",
    "network": "soroban config network add --global testnet --rpc-url https://soroban-testnet.stellar.org --network-passphrase 'Test SDF Network ; September 2015'",
    "bindings": "soroban contract bindings typescript --network testnet --overwrite -contract-id $(cat ../.soroban/auction-id) --output-dir node_modules/soroban_auction_client",
    "postinstall": "npm run network && npm run bindings"
    
  },
  "dependencies": {
    "@stellar/freighter-api": "^1.7.1",
    "astro": "^4.0.7"
  }
}
